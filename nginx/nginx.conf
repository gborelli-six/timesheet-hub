server {
    listen 80;
    client_max_body_size 10M;

    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;
  
    # Difesa in profondità: la superficie test-only non deve mai essere
    # raggiungibile in produzione, anche se la guardia applicativa fallisse.
    location /api/_test/ {
        return 404;
    }

    location /api/ {
        proxy_pass         http://backend.railway.internal:8000;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 60s;
    }

    location / {
        proxy_pass       http://frontend.railway.internal:3000;
        proxy_set_header Host $host;
    }
}
